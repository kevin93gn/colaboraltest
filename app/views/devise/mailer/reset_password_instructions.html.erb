


<%= render 'layouts/mailer_header' %>

<div style="color:#000;padding:30px 5px 10px 5px;">

  <p>Hola <%= "#{@resource.first_name} #{@resource.last_name}" %>!</p>

  <p> Para reestablecer tu contraseña debes hacer click en el siguiente enlace:</p>

  <p id="mensaje" style="border-radius:3px; -moz-border-radius:3px; -webkit-border-radius:3px;background-color:#91b356;padding:10px;text-align:center;">
    <%= link_to 'Cambiar mi password', edit_password_url(@resource, reset_password_token: @token, :style => 'color:#FFFFFF !important;') %>
  </p>
</div>

<%= render 'layouts/mailer_footer', :no_reply => true %>
